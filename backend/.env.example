DATABASE_URL="postgresql://slow:slow_password@localhost:5433/slow_oim?schema=public"
PORT=3001
API_KEY="change-me"
OWNER_EMAILS="brett@example.com"
ADMIN_EMAILS="brett@example.com"
SESSION_TTL_DAYS=14
UPLOAD_DIR="../uploads"
CORS_ORIGIN="http://127.0.0.1:8080"
# Public web app URL (email verification links). Required for signup emails.
WEB_APP_URL="http://127.0.0.1:8080"
# Optional SMTP for verification emails. If unset, the link is only logged on the server.
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="SLOW <no-reply@example.com>"
