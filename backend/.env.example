DATABASE_URL="postgresql://slow:slow_password@localhost:5433/slow_oim?schema=public"
PORT=3001
API_KEY="change-me"
OWNER_EMAILS="brett@example.com"
ADMIN_EMAILS="brett@example.com"
SESSION_TTL_DAYS=14
UPLOAD_DIR="../uploads"
CORS_ORIGIN="http://127.0.0.1:8080"
# Public web app URL (verification + password reset links). Set to your Netlify URL in production.
WEB_APP_URL="http://127.0.0.1:8080"
# Outbound email: set Resend (recommended) or SMTP. If neither is set, links are only logged — no real email.
# Resend: https://resend.com — use onboarding@resend.dev until you verify a domain, then set RESEND_FROM.
# RESEND_API_KEY="re_..."
# RESEND_FROM="SLOW <onboarding@resend.dev>"
# SMTP (alternative to Resend)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM="SLOW <no-reply@example.com>"
