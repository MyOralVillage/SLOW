Place the branding PNG here.

Required filename:
- `sierra-leone-oim-wiki.png`

This image is referenced by `web/index.html` as:
- `./brand/sierra-leone-oim-wiki.png`

When doing static deploy (SiteGround), upload this file into `slow/brand/` alongside the rest of the `web/` files.

